# Disabled while testing — re-enable when ready for releases
# name: Build and Release
#
# on:
#   push:
#     branches: [main]

name: Build and Release (disabled)

on:
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest
    permissions:
      contents: write

    steps:
      - uses: actions/checkout@v4

      - uses: oven-sh/setup-bun@v2
        with:
          bun-version: latest

      - name: Install dependencies
        working-directory: decky-plugin
        run: bun install

      - name: Build plugin
        working-directory: decky-plugin
        run: bun run build

      - name: Package plugin
        working-directory: decky-plugin
        run: |
          bun run package

      - name: Get short SHA
        id: sha
        run: echo "short=$(git rev-parse --short HEAD)" >> "$GITHUB_OUTPUT"

      - name: Create Release
        uses: softprops/action-gh-release@v2
        with:
          tag_name: build-${{ steps.sha.outputs.short }}
          name: Build ${{ steps.sha.outputs.short }}
          body: |
            Automated build from commit ${{ github.sha }}.

            ## Installation
            1. Download `OneXPlayer_Super_X_Tools.zip`
            2. Extract to `~/homebrew/plugins/`
            3. Restart Decky: `sudo systemctl restart plugin_loader.service`
          files: decky-plugin/OneXPlayer_Super_X_Tools.zip
          make_latest: true
